@model TeamEntityCollection


<div class="row-fluid sortable">
    <div class="box span12">
        <div class="box-header well" data-original-title>
            <a href ="@Url.Action("Create", "Teams", new { area = "Addmein" })" class="btn btn-small btn-success btn-round" style="float: right; margin-right: 10px;">Add new</a>
            <h2><i class="icon icon-red icon-users"></i>Team</h2>
            <p style="float: right; margin: 5px 20px 0px 0px;">Count - <a>@Model.AllCount</a> </p>
        </div>
        <div class="box-content">
            <table class="table table-striped table-bordered bootstrap-datatable datatable">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Position</th>
                        <th style="width: 100px; text-align: center;">Create date</th>
                        <th>Main photo</th>
                        <th style="width: 20%; text-align: center;">Operations</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Teams)
                    {
                        <tr>
                            <td style="vertical-align: middle;">
                                <a href="@Url.Action("View", "Teams", new { id = item.Id, area = "Addmein" })" >@item.Name</a>
                            </td>
                            <td style="vertical-align: middle;">
                                <a href="@Url.Action("View", "Teams", new { id = item.Id, area = "Addmein" })">@item.Description</a>
                            </td>
                            <td style="width: 100px; text-align: center; vertical-align: middle;">
                                @item.CreateDate.ToString("yyyy-MM-dd", System.Globalization.CultureInfo.InvariantCulture)
                            </td>
                            <td style="vertical-align: middle;">
                                @if (!string.IsNullOrEmpty(item.ImageName))
                                {
                                    <img src="@Url.Content("~/_Files/Images/Team/64/" + item.ImageName)" alt="" width="64" />
                                }
                            </td>
                            <td class="center" style="width: 20%; text-align: center; vertical-align: middle;">
                                <a class="btn btn-danger" onclick="confirmDelete(@item.Id)"  style="">
                                    <i class="icon-trash icon-white"></i>
                                    Delete
                                </a>
                                <a class="btn btn-info" href="@Url.Action("Edit", "Teams", new { id = item.Id })" >
                                    <i class="icon-edit icon-white"></i>
                                    Edit
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script>
    function confirmDelete(id) {
        if (confirm("Are you sure to delete this member ?")) {
            document.location.href = '@Url.Action("Delete", "Teams", new { area = "Addmein" })/' + id;
        }
    }
</script>


