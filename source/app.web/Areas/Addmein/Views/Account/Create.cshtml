@model  User
<div class="row-fluid sortable">
    <div class="box span12">
        <div class="box-header well" data-original-title>
            <h2><i class="icon icon-red icon-plus"></i>New User</h2>
        </div>
        <div class="box-content">
            @using (Html.BeginForm("Create", "Account", FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
            {
                <fieldset>
                    <div class="control-group">
                        <label class="control-label" for="focusedInput">Username <span style="color: #ee5f5b;">*</span></label>
                        <div class="controls">
                            @Html.TextBoxFor(model => model.Username)
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label" for="focusedInput">Password <span style="color: #ee5f5b;">*</span></label>
                        <div class="controls">
                            @Html.TextBoxFor(model => model.Password)
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label" for="focusedInput">Role <span style="color: #ee5f5b;">*</span></label>
                        <div class="controls">
                            @Html.DropDownListFor(model => model.Role, ViewBag.Roles as IEnumerable<SelectListItem>, new { data_rel = "chosen", @id = "role", @style = "" })
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label" for="focusedInput">Full name <span style="color: #ee5f5b;">*</span></label>
                        <div class="controls">
                            @Html.TextBoxFor(model => model.Fullname)
                        </div>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Save</button>
                        <a class="btn btn-danger" href="@Url.Action("List", "Account")">Cancel</a>
                    </div>
                </fieldset>
            }
        </div>
    </div>
</div>
<script>
    jQuery(document).ready(function () {
        $("#role").chosen();
    });
</script>
